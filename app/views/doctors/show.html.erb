<h2><%= @doctor.name %></h2>

<p><%= @doctor.department %></p>

<h3>Appointments</h3>
<ul>
  <% @doctor.appointments.each do |app| %>
    <li><%= link_to app.patient.name, patient_path(app.patient) %> - <%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M") %></li>
  <% end %>
</ul>